<%- include('partials/header') %>

<h1>Billing Analytics</h1>

<table>
  <thead>
    <tr>
      <th>Month</th>
      <th>Completed Subscriptions</th>
    </tr>
  </thead>
  <tbody>
    <% monthlyData.forEach(row => { %>
      <tr>
        <td><%= row.month %></td>
        <td><%= row.events %></td>
      </tr>
    <% }) %>
  </tbody>
</table>

<%- include('partials/footer') %>