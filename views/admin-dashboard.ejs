<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/admin.css">
</head>
<body>
  <div class="glass-card">
    <h1>Welcome, <%= user.username %> 👋</h1>

    <p class="text-gray-300 mb-6">You are logged in as <span class="font-bold"><%= user.role %></span>.</p>

    <form action="/admin/logout" method="post">
      <button type="submit" class="bg-red-500 hover:bg-red-600">Logout</button>
    </form>

    <h2 class="text-white text-2xl font-bold mt-8 mb-4">Contact Submissions</h2>

    <% if (contacts.length === 0) { %>
      <p class="text-gray-300">No contact submissions yet.</p>
    <% } else { %>
      <div class="overflow-x-auto">
        <table class="min-w-full text-white">
          <thead>
            <tr class="bg-gray-700 text-left">
              <th class="p-2">Name</th>
              <th class="p-2">Email</th>
              <th class="p-2">Phone</th>
              <th class="p-2">Message</th>
              <th class="p-2">Created At</th>
            </tr>
          </thead>
          <tbody>
            <% contacts.forEach(contact => { %>
              <tr class="border-t border-gray-600 hover:bg-gray-800">
                <td class="p-2"><%= contact.name %></td>
                <td class="p-2"><%= contact.email %></td>
                <td class="p-2"><%= contact.phone %></td>
                <td class="p-2"><%= contact.message %></td>
                <td class="p-2"><%= new Date(contact.created_at * 1000).toLocaleString() %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>
  </div>
</body>
</html>