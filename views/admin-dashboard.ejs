<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin Dashboard | TruView CMS</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/admin.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-gray-900 to-gray-700 text-white p-8">

  <div class="max-w-4xl mx-auto bg-white bg-opacity-10 backdrop-blur-lg p-8 rounded-2xl shadow-2xl">
    <h1 class="text-4xl font-bold mb-6">👋 Welcome, <%= user.username %></h1>
    <p class="mb-6">You are logged in as <span class="font-bold"><%= user.role %></span>.</p>

    <form action="/admin/logout" method="POST" class="mb-6">
      <button type="submit" class="bg-red-600 hover:bg-red-800 text-white py-2 px-4 rounded font-bold">
        Logout
      </button>
    </form>

    <h2 class="text-2xl font-bold mb-4">📬 Contact Submissions</h2>
    <% if (contacts.length === 0) { %>
      <p class="text-gray-300">No contact submissions yet.</p>
    <% } else { %>
      <div class="space-y-4">
        <% contacts.forEach(contact => { %>
          <div class="p-4 bg-gray-800 rounded shadow">
            <p><span class="font-bold">Name:</span> <%= contact.name %></p>
            <p><span class="font-bold">Email:</span> <%= contact.email %></p>
            <p><span class="font-bold">Phone:</span> <%= contact.phone %></p>
            <p><span class="font-bold">Message:</span> <%= contact.message %></p>
            <p class="text-xs text-gray-400">Submitted at: <%= new Date(contact.created_at * 1000).toLocaleString() %></p>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

</body>
</html>