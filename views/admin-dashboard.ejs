<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/header') %>
  <title>Admin Dashboard - TruView CMS</title>
</head>
<body class="bg-gradient-to-br from-gray-800 to-black min-h-screen text-white p-8">
  <div class="glass p-8 rounded-2xl shadow-2xl max-w-4xl mx-auto">
    <h1 class="text-3xl font-bold mb-6">Welcome, <%= user.username %> 👋</h1>
    <p class="mb-6 text-gray-300">You are logged in as <strong class="text-green-400"><%= user.role %></strong>.</p>

    <form action="/admin/logout" method="POST" class="mb-8">
      <button type="submit" class="bg-red-600 hover:bg-red-800 text-white font-bold py-2 px-4 rounded-xl">Logout</button>
    </form>

    <h2 class="text-2xl font-semibold mb-4">📬 Contact Submissions</h2>

    <% if (contacts.length === 0) { %>
      <p class="text-gray-400">No contact submissions yet.</p>
    <% } else { %>
      <div class="space-y-4">
        <% contacts.forEach(contact => { %>
          <div class="p-4 border border-gray-700 rounded-xl">
            <p><strong>Name:</strong> <%= contact.name %></p>
            <p><strong>Email:</strong> <%= contact.email %></p>
            <p><strong>Phone:</strong> <%= contact.phone %></p>
            <p><strong>Message:</strong> <%= contact.message %></p>
            <p class="text-xs text-gray-400">Submitted: <%= new Date(contact.created_at * 1000).toLocaleString() %></p>
          </div>
        <% }); %>
      </div>
    <% } %>
  </div>
</body>
</html>